[package]
name = "lib2"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]

[dependencies]
anyhow.workspace = true
rian.workspace = true
serde = { workspace = true, features = ["derive", "rc", "std"] }
